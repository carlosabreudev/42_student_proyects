/**
 * Función que reserva (utilizando malloc(3)) un array de strings resultante de separar la string ’s’ en substrings
 * utilizando el caracter ’c’ como delimitador. El array debe terminar con un puntero NULL.
 * @param s La string a separar.
 * @param c El carácter delimitador.
 * @return El array de nuevas strings resultante de la separación. NULL si falla la reserva de memoria.
*/
char **ft_split(char const *s, char c)
{
    
}